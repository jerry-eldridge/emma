//
// defs.h
//
#ifndef DEFS_H
#define DEFS_H

const unsigned MAXSIZE = 40; 
	// sets maximum size of MAXSIZE x MAXSIZE array
	// (40 x 40 = 800 doubles, Using double as 4 bytes,
	// About 3.2 KB per array, so could increase MAXSIZE)
#define ABS(x)	(((x) >= 0) ? (x) : -(x)) 

#endif

