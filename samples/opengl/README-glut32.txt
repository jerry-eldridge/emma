To get OpenGL working, I used GLUT (freeglut) and the glut32.dll
is from the freeglut (search web for download) project.

Though I include compiled .exe versions of the programs to running.
The use glut32.dll which should be in the path or run directory.